{% set _currentRoute = app.request.attributes.get('_route') %}
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container container-fluid">
        <a class="navbar-brand" href="{{ path('app_index') }}">ActuFoot</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar links -->
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link {% if _currentRoute == 'app_index' %}{{ 'active' }}{% endif %}" href="{{ path("app_index") }}">Liste équipes/joueurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if _currentRoute == 'app_create_team' %}{{ 'active' }}{% endif %}" href="{{ path('app_create_team') }}">Ajouter équipe/joueur</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if _currentRoute == 'app_buy_or_sell_player' %}{{ 'active' }}{% endif %}" href="{{ path('app_buy_or_sell_player') }}">Vente/Achat joueur</a>
                </li>
            </ul>
        </div>
    </div>
</nav>